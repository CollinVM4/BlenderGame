--!strict
return {
    -- Studs between the head top and the lowest ingredient corner.
    HeightAboveHead = 0.35,
    -- Positive is forward (character-local -Z).
    ForwardOffset = 0,
    RotationDegrees = Vector3.new(0, 0, 0),
}
